{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": [
    "Enchanted_Games"
  ],
  "contact": {
    "sources": "${source_url}",
    "issues": "${issue_tracker}"
  },
  "license": "${license}",
  "icon": "${icon}",
  "environment": "client",
  "entrypoints": {
    "main": [
      "games.enchanted.${id}.fabric.FabricCommonEntry"
    ],
    "client": [
      "games.enchanted.${id}.fabric.FabricClientEntry"
    ],
    "modmenu": [
      "games.enchanted.${id}.fabric.ModMenuIntegration"
    ]
  },
  "accessWidener": "${id}.accesswidener",
  "mixins": [
    {
      "config": "${id}.mixins.json"
    }
  ],
  "depends": {
    "minecraft": "${minecraft}",
    "fabricloader": "${fabric_loader_dep_str}"
  }
}